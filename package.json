{
  "name": "blueprint-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:create": "wrangler d1 create blueprint-habit",
    "db:migrate:local": "wrangler d1 migrations apply blueprint-habit --local",
    "db:migrate:remote": "wrangler d1 migrations apply blueprint-habit --remote"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.78.12"
  }
}